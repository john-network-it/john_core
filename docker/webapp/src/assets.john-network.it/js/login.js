var input = document.getElementById("password");
var noticeSend = false;

/**input.addEventListener("keyup", function(event) {
 * if(event.getModifierState("CapsLock")) {
 *   if(noticeSend == false) {
 *       console.log("WARNING CAPS");
 *       noticeSend = true;
 *   } else {
 *       noticeSend = false;
 *   }
 * }
 *});
 */